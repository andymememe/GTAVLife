{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "${workspaceFolder}/.vscode/script/build.ps1",
            "type": "shell",
            "args": [
                "${workspaceFolder}"
            ],
            "group": "build",
            "presentation": {
                "reveal": "silent"
            }
        }
    ]
}